- 'select * from `solicitacoes` inner join `processos` on `solicitacoes`.`processo_id` = `processos`.`id` inner join `usuarios` as `solicitantes` on `solicitacoes`.`solicitante_id` = `solicitantes`.`id` left join `usuarios` as `recebedores` on `solicitacoes`.`recebedor_id` = `recebedores`.`id` left join `usuarios` as `remetentes` on `solicitacoes`.`remetente_id` = `remetentes`.`id` left join `usuarios` as `rearquivadores` on `solicitacoes`.`rearquivador_id` = `rearquivadores`.`id` inner join `lotacoes` as `destinatarias` on `solicitacoes`.`lotacao_destinataria_id` = `destinatarias`.`id` order by `solicitada_em` asc, `entregue_em` asc, `devolvida_em` asc, `por_guia` asc, `processos`.`numero` desc, `solicitantes`.`username` desc, `recebedores`.`username` desc, `remetentes`.`username` desc, `rearquivadores`.`username` desc, `destinatarias`.`sigla` desc, `id` desc'
- {  }
